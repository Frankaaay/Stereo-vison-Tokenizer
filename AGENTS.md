# AGENTS.md

## 授权与范围边界

- 用户当前任务是唯一范围。对不改变目标、结果语义或外部状态的操作性小问题，可直接纠正并继续。
- 已获授权范围内的局部、低风险、可回退修正和定向测试可以直接完成；不得借此增加新功能、公共接口、依赖、数据格式、跨模块重构、训练/评估启动或远端写操作。
- 只有当问题显著影响技术路线、结果语义、实验可比性、数据或资产安全，或需要新的实质性选择与授权时，才暂停并询问。典型情况包括合同冲突、目标 Git 状态不符、关键环境/数据/checkpoint 缺失、GPU 不可用、可能覆盖产物或消耗未授权资源。
- 暂停前先完成解释问题所需的最小只读取证，并说明证据、影响、不修复的后果、推荐方案、拟修改文件、验证方式、风险和回退办法。
- 修改前先查已有实现和相邻代码，采用完成需求的最小改动。无关且不阻塞的问题只记录和报告，不顺带修改。
- 不留下占位实现、伪代码或用 `TODO` 替代现有逻辑；不做无关格式化、重命名、搬移或清理。

## Git、代码修改与文件安全

- 修改前检查 `git status --short --branch`、当前分支和 HEAD；保留并避让用户已有修改。
- 未经用户在当前任务中明确授权，不创建任何本地或远端 branch/worktree，也不通过 GitHub、服务器脚本或其他方式间接创建。
- 用户指定目标分支或现有 worktree 后，所有开发、测试、评估和训练均使用该目标。目标不存在、存在未提交修改、被其他 worktree 占用、无法 fast-forward 或不适合直接修改时，保持现场并询问用户，不创建替代目标绕过。
- 仓库源码、脚本、配置和测试只在用户指定的本地 worktree 修改。标准流程为：本地修改与最小验证 → 经授权 commit/push → 服务器核对 clean 状态、分支和 HEAD → fast-forward 到已推送的精确 SHA → 执行已授权测试或运行。
- 服务器 clone 不现场编辑、commit 或 push。服务器无法 fast-forward、HEAD 与目标 SHA 不一致或 origin/upstream 不符合当前用户和项目合同时，立即停止。
- 跨用户 checkpoint、数据和模型资产仅在用户已授权的明确绝对路径下作为只读输入；代码、Git 元数据、运行输出、日志和临时文件写入当前执行用户自己的路径。
- 不丢弃未提交修改，不执行 `git reset --hard`、`git checkout -- .` 或 `git clean -fd`，除非用户明确要求。
- 禁止批量删除文件或目录；只能一次删除一个明确文件。不得使用 `rm -rf`、`Remove-Item -Recurse`、`del /s`、`rd /s` 或 `rmdir /s`。
- 只暂存具名文件，不使用 `git add .` 或 `git add -A`。未经明确要求，不 commit、不 push、不创建 PR。

## 服务器操作

- H100 和 H200 操作统一由本地 Codex 通过本机 SSH 发起和管理。
- 操作 H100 前完整阅读 [`doc/H100服务器使用指南.md`](doc/H100服务器使用指南.md)。
- 操作 H200 前完整阅读 [`doc/H200-qinghua服务器使用指南.md`](doc/H200-qinghua服务器使用指南.md)。
- 服务器拓扑、登录、Git 同步、存储与数据资产、运行环境、调度和启动要求以对应指南为准。
- 指南不构成安装、远端修改、提交或取消作业、训练、评估、预处理、覆盖产物、删除文件或扩大资源规模的授权；当前用户指令和本文件的授权边界优先。
- 分支、SHA、worktree、GPU、数据、checkpoint、输出、进程和日志等易变事实必须实时核对，不用历史记录覆盖当前证据。

## 验证、实验记录与长任务

- 修复优先复现并定位根因，测试只覆盖被改行为和关键边界；本地环境缺少运行依赖时，明确区分静态检查与服务器运行证据。
- 代码、脚本、配置、实验设置或排查结论发生实质修改，或完成训练、评估、调试实验和关键测试后，同步更新 `docs/YY-MM-DD/YY-MM-DD-<topic>.md`。
- 实验记录至少包含目的、分支/commit、运行位置、关键命令、checkpoint/output/log 路径、主要指标、异常、结论和下一步；大日志、checkpoint、dataset 和生成视频不写入仓库文档。
- 正在运行的任务记录 tmux/session、日志、当前状态和结果路径。每次启动长任务后，在文档和用户回复中分别给出主体完成与 checkpoint/结果验证等后处理的 ETA，并注明估算时刻和依据。
- 尚无足够真实吞吐时只给历史实测区间并标为初估，不等待第二个采样点；实际吞吐、任务范围或重试变化时再刷新 ETA。
- 除非用户明确要求持续监控，长任务启动后只做一次健康检查：进程或 tmux、GPU 占用、日志首个 step/heartbeat 和即时异常，然后报告状态与 ETA 并结束当前轮次。
- 状态查询每轮只做一次最小快照，尽量合并为一个远端命令，原则上不超过 3 次查询；不使用 `sleep`、`tail -f` 或轮询循环等待变化。
- 仅当任务预计 2 分钟内完成或用户明确要求当前轮等待时，才进行有限复查。
- SSH 255、连接中断、等待失败或返回缺失时，先只读核对远端是否落地；只有确认未落地且重试幂等时才能继续。

## 交付检查

- 最终回复前确认：只修改必要文件；没有未请求能力、重复实现、临时文件、无关格式化或未验证假设。
- 若有代码、脚本、配置或实验修改，确认对应实验文档已同步；若无需记录，说明原因。
- 报告实际验证结果和未完成的运行门禁，不把静态检查、smoke、文件名或 run 名称描述为更强证据。
