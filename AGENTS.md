# AGENTS.md

## 任务确认与范围边界

- 用户当前任务是唯一范围。每个新任务开始时，先完成必要的最小只读检查，再向用户确认最重要的目标、边界、验收标准和会改变技术路线的选择；用户已经明确或可从环境直接确认的事实不重复询问。
- 有任何影响结果语义、实验可比性、数据安全、资源消耗或外部状态的疑问，必须在写入、运行或提交前询问；不自行补充假设或扩大授权。
- 修改前先查已有实现和相邻代码，采用完成需求的最小改动。无关且不阻塞的问题只记录和报告，不顺带修改。
- 不新增未请求的功能、入口、配置、依赖、公共接口、数据格式、脚本、文档或抽象层；确有必要时先说明原因、影响和拟修改范围，等待确认。
- 不留下占位实现、伪代码或用 `TODO` 替代现有逻辑；不做无关格式化、重命名、搬移或清理。

## 开发环境与服务器

- 本项目不设置统一的个人开发位置、SSH 账号、用户目录或数据路径；本机或服务器开发方式及私人资产约定只记录在对应个人服务器指南中。
- 操作服务器前，根据当前 SSH config 的实际账号完整阅读对应用户目录下的两份指南：
  - `hezhou`：[H100 服务器使用指南](doc/hezhou/H100服务器使用指南.md)、[H200-qinghua 服务器使用指南](doc/hezhou/H200-qinghua服务器使用指南.md)。
  - `Frank`/`frank`：[H100 服务器使用指南](doc/frank/H100服务器使用指南.md)、[H200-qinghua 服务器使用指南](doc/frank/H200-qinghua服务器使用指南.md)。
- 无法确认当前账号或账号未列出时，先只读核对并向用户确认；不使用其他用户目录中的代码、环境或资产作为运行来源。
- H100 和 H200 仅在当前任务明确授权时用于对应测试、训练、评估、预处理或数据操作。
- 服务器拓扑、登录方式、Git 状态、GPU、数据、checkpoint、输出、进程、日志和资源状态均为易变事实，必须实时核对，不用文档或历史记录替代当前证据。
- 指南不构成安装、远端写入、作业提交/取消、训练、评估、预处理、覆盖、删除或扩大资源的授权；当前用户指令和本文件的边界优先。

## Git 与修改流程

- 在目标仓库运行代码、修改代码、测试、训练、评估或预处理前，必须先执行：

  ```bash
  git status --short --branch
  git fetch origin
  git pull --ff-only origin "$(git branch --show-current)"
  ```

- `git status` 非 clean、当前分支为空或不符合目标、fetch/pull 失败、无法 fast-forward 时，保持现场并向用户说明，不基于旧代码继续，也不创建旁路 worktree 绕过。
- 用户指定目标分支或 worktree 后，所有开发、测试和运行均使用该目标。确需 worktree 且已获授权时，统一放在仓库 `.worktree/` 下。
- 未经当前任务明确授权，不创建本地或远端 branch/worktree，不 commit、push 或创建 PR。
- 不丢弃未提交修改，不执行 `git reset --hard`、`git checkout -- .` 或 `git clean -fd`，除非用户明确要求。
- 禁止对宽泛目录执行批量递归删除；只能操作用户授权的精确目标，不使用 `rm -rf` 等命令删除仓库、home、数据根或共享目录。
- 只暂存具名文件，不使用 `git add .` 或 `git add -A`。
- 提交信息使用中文 conventional commits，例如 `fix: 修复 motion manifest 读取`。
- 纯文档修改默认只保留在当前开发 worktree；只有用户明确授权时才 commit/push。代码修改完成后先做可行的最小验证，再按授权 commit/push。
- 不把其他用户的 clone、Git 元数据、alternate 或工作目录作为当前用户仓库的写入来源。跨用户资产仅在用户授权的明确绝对路径下作为只读输入。

## 数据与大文件安全

- `datasets` 目录只放数据，不放代码、脚本、虚拟环境、checkpoint、日志、缓存或临时工程文件。
- 仓库中不提交数据集、checkpoint、wandb、runs、logs、虚拟环境、缓存目录或生成的大型产物。
- 训练和评估通过服务器既有绝对路径、symlink 或环境变量引用数据与大文件；不把大产物复制进 Git。
- 在挂载 KS3/KPFS 的服务器上，禁止使用 `du`、通配递归搜索或并发扫描直接遍历整个 `DATA` 根。扫描本地盘时把范围精确限定到 `DATA/disk1`；访问 KPFS/KS3 时必须使用精确前缀、浅层、单线程查询，不直接深度扫描。
- 共享数据集和缓存按对应服务器指南处理；不覆盖、改名或删除他人正在使用的数据，不把节点临时盘作为唯一结果存储。
- 删除、覆盖、上传、下载、数据迁移和大规模扫描都需要当前任务的明确授权与精确目标。

## 实现与测试约束

- 优先复用已有优秀仓库和项目现有实现作为 baseline，不从头重复实现已有能力。
- 采用单一生产路径，不新增冗余开关、fallback、兼容旁路或重复工具函数。
- 只改直接相关函数、调用点和测试；新文件、新依赖、新公共接口、新数据格式和跨模块重构均视为范围扩大。
- 修 bug 时先复现并定位根因，补丁小而定向。只为验证设计的 fail-fast/check/smoke 不进入生产路径；可合入的检查和 smoke 放在 `tests/`，纯实验代码放专门分支且不并入 `main` 或生产分支。
- 发现无关问题时只报告路径、现象和建议；除非用户明确要求，不顺手修改。
- 若 diff 明显扩大、需要触碰无关模块或需求出现歧义，立即暂停并说明范围变化。

## 验证、实验记录与长任务

- 测试只覆盖被改行为和关键边界；缺少运行依赖或硬件时，明确区分静态检查、smoke 和真实运行证据。
- 每次代码、脚本、配置、实验设置或排查结论发生实质修改后，同步更新 `docs/YY-MM-DD/YY-MM-DD-<topic>.md`。
- 每次训练、评估、调试实验或关键测试后记录：目的、分支/commit、运行位置、关键命令、checkpoint/output/log 路径、主要指标、异常、结论和下一步；不把大日志、checkpoint、dataset 或生成视频写入文档。
- 正在运行的任务记录状态、Job ID 或 tmux/session、日志和结果路径。长任务启动后只做一次健康检查，并报告主体完成与后处理 ETA；除非用户明确要求，不循环轮询。
- SSH 中断或返回缺失时，先只读核对操作是否落地；确认未落地且重试幂等后才能继续。

## 交付检查

- 最终回复前确认只修改必要文件，没有未请求能力、重复代码、临时文件、无关格式化或未验证假设。
- 若有代码、脚本、配置或实验修改，确认对应 `docs/YY-MM-DD/` 文档已同步；确实无需记录时说明原因。
- 报告实际验证结果和未完成门禁，不把静态检查或 smoke 描述为完整验证。
