# S48 停止与恢复

用户于 2026-09-08 要求停止 S48 腾出 H200-1；已发送中断并确认全部训练进程和
tmux 退出、8 GPU 显存为 0。日志最后进度 23489/40000，原产物保留。

用户随后授权加载 checkpoint 重启。最新 last.ckpt 实读 generator_updates=22000，
global_step=22000，包含 1 个 optimizer state 和 scheduler state，输入合同 correct。
源路径：`/data/home/frank/experiments/stereo-input-ablation-s48-h2001-20260907-v1/stereo-vae/ubyy2onz/checkpoints/last.ckpt`。
未保存的约 1489 updates 需重跑。使用 resume_from_checkpoint 恢复完整状态，
总目标仍为 40000（剩余 18000），保留 batch 192:40:160:36、GA1、8 H200 BF16、
seed 1234 和原始数据、loss、teacher、LR 合同。

启动入口：`doc/frank/h2001-s48-resume-20260908.sh`。
独立输出：`/data/home/frank/experiments/stereo-input-ablation-s48-h2001-20260908-resume-v1`。
tmux：`s48-resume-h2001-20260908-v1`。原输出与 checkpoint 不覆盖。
本地 Plan.md 的未提交用户修改按先前授权保留。

## 恢复结果

启动 SHA `9d4240b9c5c499600bda3731a792c3af3b5222b7`，H200-1 clean 且同步到该版本。
2026-09-08 18:52:20 CST 启动。Shell 语法与 staged diff 检查通过。
resolved config 相对原训练仅改变输出目录、resume checkpoint 和调度起点 22000。
日志确认 `Restored all states from the checkpoint`，随后出现 22001、22002、22003
训练更新，无 traceback/OOM/CUDA error/non-finite。
Lightning 提示 dataloader 不支持通用 state_dict 恢复；本项目已通过
mode_schedule_start_update=22000 向 MixedModeBatchSampler 传入 start_update，
恢复既有模式调度位置。本次未另行验证逐像素随机状态的完全可复现性。
预计剩余约 14–16 小时（按原运行速度估算），无独立后处理任务。
